import {FiArrowUpRight,FiGrid} from "react-icons/fi";import {Reveal,SectionHeading} from "./common";import {Link} from "react-router-dom";import useLiveHomeData from "../hooks/useLiveHomeData";
const emptyCategories={data:[]};
export default function CategoryGrid(){const response=useLiveHomeData("/home/categories",emptyCategories),rows=response?.data??null;return <section id="categories" className="py-20"><div className="section-shell"><SectionHeading eyebrow="Explore" title="Shop by Category" copy="Browse active categories managed by MB Store."/>{rows===null?<div className="h-40 animate-pulse rounded-2xl bg-slate-100"/>:rows.length?<div className="grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-6">{rows.map((c,i)=><Reveal key={c.id} delay={(i%6)*.04}><Link to={`/catalogue?categoryId=${c.id}`} className="group block overflow-hidden rounded-2xl border bg-white text-center shadow-sm transition hover:-translate-y-1">{c.imageUrl?<img src={c.imageUrl} alt={c.name} loading="lazy" className="aspect-[4/3] w-full object-cover"/>:<span className="m-5 mx-auto grid aspect-[4/3] place-items-center rounded-2xl bg-sky-50 text-3xl text-sky-600"><FiGrid/></span>}<div className="p-4"><h3 className="font-black">{c.name}</h3><p className="mt-1 text-xs text-slate-500">{c.productCount} Products</p><FiArrowUpRight className="mx-auto mt-3 text-sky-600"/></div></Link></Reveal>)}</div>:<p className="rounded-2xl bg-slate-50 p-10 text-center">No categories available.</p>}</div></section>}
