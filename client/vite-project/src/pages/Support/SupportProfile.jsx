import {useEffect,useState} from 'react';import {supportApi} from '../../services/supportService';
export default function SupportProfile(){const [profile,setProfile]=useState(null),[saving,setSaving]=useState(false);useEffect(()=>{supportApi.profile().then(({data})=>setProfile(data.profile))},[]);if(!profile)return <div className="h-64 animate-pulse rounded-2xl bg-white"/>;const save=async e=>{e.preventDefault();setSaving(true);await supportApi.updateProfile({name:profile.name,gender:profile.gender});setSaving(false)};return <><p className="text-xs font-black uppercase tracking-widest text-teal-700">Account</p><h1 className="mt-1 text-3xl font-black">Support Profile</h1><form onSubmit={save} className="mt-6 max-w-2xl rounded-2xl border bg-white p-6 shadow-sm"><div className="grid gap-5 sm:grid-cols-2"><Field label="Name"><input value={profile.name} onChange={e=>setProfile({...profile,name:e.target.value})} className="input"/></Field><Field label="Gender"><select value={profile.gender||''} onChange={e=>setProfile({...profile,gender:e.target.value})} className="input"><option>Male</option><option>Female</option><option>Other</option></select></Field>{[['Email',profile.email],['Support ID',profile.supportId],['Role',profile.role],['Account Status',profile.status],['Joined',new Date(profile.joinedDate).toLocaleDateString()]].map(([a,b])=><div key={a}><p className="text-xs font-bold uppercase text-slate-400">{a}</p><b className="mt-1 block capitalize">{b}</b></div>)}</div><button disabled={saving} className="mt-6 rounded-xl bg-teal-700 px-5 py-3 font-bold text-white">{saving?'Saving...':'Save Profile'}</button></form></>}
const Field=({label,children})=><label className="block text-sm font-bold">{label}{children}</label>;
