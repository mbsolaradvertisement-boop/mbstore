import { useEffect, useState } from "react";
import api from "../../lib/api";
export default function SupportProfile(){const [profile,setProfile]=useState(null);useEffect(()=>{api.get("/support/profile").then(({data})=>setProfile(data.profile))},[]);return <><p className="text-xs font-black uppercase tracking-widest text-teal-700">Account</p><h1 className="mt-2 text-3xl font-black">Profile</h1><section className="mt-8 max-w-2xl rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">{profile? <dl className="grid gap-6 sm:grid-cols-2">{[["Name",profile.name],["Email",profile.email],["Gender",profile.gender||"Not specified"],["Role",profile.role],["Account Status",profile.status]].map(([label,value])=><div key={label}><dt className="text-xs font-bold uppercase tracking-wide text-slate-400">{label}</dt><dd className="mt-1 font-bold capitalize">{value}</dd></div>)}</dl>:<p className="text-slate-500">Loading profile...</p>}</section></>}
