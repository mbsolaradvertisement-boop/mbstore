import { useCallback, useEffect, useState } from "react";
import { FiChevronLeft, FiChevronRight, FiEdit2, FiEye, FiSearch, FiTrash2, FiUsers } from "react-icons/fi";
import api, { apiMessage } from "../../lib/api";
import { useToast } from "../../context/ToastContext";
import DashboardShell from "../Dashboards/DashboardShell";

const sellerColumns = [["sellerId","Seller ID"],["sellerName","Seller Name"],["companyName","Company"],["email","Email"],["businessEmail","Business Email"],["phoneNumber","Phone"],["gst","GST"],["state","State"],["district","District"],["verificationStatus","Verification"]];
const customerColumns = [["customerId","Customer ID"],["customerName","Customer Name"],["email","Email"],["phoneNumber","Phone"],["address","Address"],["state","State"],["district","District"],["area","Area"],["landmark","Landmark"]];
const tone = value => value === "Verified" ? "bg-emerald-50 text-emerald-700" : value === "Rejected" || value === "Inactive" ? "bg-red-50 text-red-700" : "bg-amber-50 text-amber-700";

export default function ProfileManagement({ type }) {
  const seller=type==="sellers", columns=seller?sellerColumns:customerColumns, {toast}=useToast();
  const [rows,setRows]=useState([]), [loading,setLoading]=useState(true), [search,setSearch]=useState(""), [query,setQuery]=useState(""), [status,setStatus]=useState(""), [completion,setCompletion]=useState(""), [sort,setSort]=useState("newest"), [page,setPage]=useState(1), [meta,setMeta]=useState({totalPages:1,totalRecords:0});
  useEffect(()=>{const timer=setTimeout(()=>{setQuery(search);setPage(1)},350);return()=>clearTimeout(timer)},[search]);
  const load=useCallback(async()=>{setLoading(true);try{const {data}=await api.get(`/admin/${type}`,{params:{search:query||undefined,status:status||undefined,completion:completion||undefined,sort,page,limit:20}});setRows(data.data);setMeta(data)}catch(error){toast(apiMessage(error),"error")}finally{setLoading(false)}},[type,query,status,completion,sort,page,toast]); useEffect(()=>{load()},[load]);
  const change=(setter)=>(event)=>{setter(event.target.value);setPage(1)};
  return <DashboardShell><div className="mx-auto max-w-[1700px]"><div className="mb-6"><p className="text-xs font-black uppercase tracking-[.16em] text-teal-700">Admin management</p><h1 className="mt-1 text-3xl font-black">{seller?"Seller":"Customer"} Management</h1><p className="mt-1 text-sm text-slate-500">{meta.totalRecords} records{meta.totalRecords===1?"":"s"}</p></div><section className="rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-wrap gap-3 border-b border-slate-100 p-4"><label className="flex min-w-64 flex-1 items-center gap-2 rounded-xl border border-slate-200 px-4 py-2.5"><FiSearch className="text-slate-400"/><input value={search} onChange={e=>setSearch(e.target.value)} className="w-full text-sm outline-none" placeholder={`Search ${type} by name, ID, email or phone...`}/></label><select value={status} onChange={change(setStatus)} className="rounded-xl border border-slate-200 px-3 text-sm"><option value="">All statuses</option>{["Verified","Pending","Rejected","Inactive"].map(x=><option key={x}>{x}</option>)}</select><select value={completion} onChange={change(setCompletion)} className="rounded-xl border border-slate-200 px-3 text-sm"><option value="">All profiles</option><option value="completed">Completed</option><option value="incomplete">Incomplete</option></select><select value={sort} onChange={change(setSort)} className="rounded-xl border border-slate-200 px-3 text-sm"><option value="newest">Newest</option><option value="oldest">Oldest</option></select></div><div className="overflow-x-auto"><table className="w-full min-w-[1250px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-400"><tr>{columns.map(([,label])=><th key={label} className="px-4 py-3">{label}</th>)}<th className="px-4">Completion</th><th className="px-4">Status</th><th className="px-4">Created</th><th className="px-4">Actions</th></tr></thead><tbody className="divide-y divide-slate-100">{loading?Array.from({length:6},(_,i)=><tr key={i}>{Array.from({length:columns.length+4},(_,j)=><td key={j} className="px-4 py-4"><div className="h-4 animate-pulse rounded bg-slate-200"/></td>)}</tr>):rows.length===0?<tr><td colSpan={columns.length+4} className="py-16 text-center"><FiUsers className="mx-auto text-3xl text-slate-300"/><p className="mt-3 font-bold text-slate-600">{query?"No results found for your search.":`No ${type} found.`}</p></td></tr>:rows.map(row=><tr key={row.id} className="hover:bg-slate-50">{columns.map(([key])=><td key={key} className="max-w-56 truncate px-4 py-4" title={row[key]||""}>{row[key]||"—"}</td>)}<td className="px-4"><div className="flex items-center gap-2"><div className="h-2 w-16 rounded-full bg-slate-100"><div className="h-full rounded-full bg-teal-600" style={{width:`${row.profileCompletion}%`}}/></div><b>{row.profileCompletion}%</b></div></td><td className="px-4"><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${tone(row.status)}`}>{row.status}</span></td><td className="whitespace-nowrap px-4">{new Date(row.createdAt).toLocaleDateString()}</td><td className="px-4"><div className="flex gap-2"><button title="View" className="rounded-lg border p-2"><FiEye/></button><button title="Edit" className="rounded-lg border p-2 text-blue-600"><FiEdit2/></button><button title="Deactivate" className="rounded-lg border p-2 text-red-600"><FiTrash2/></button></div></td></tr>)}</tbody></table></div><div className="flex items-center justify-between border-t p-4"><p className="text-sm text-slate-500">Page {page} of {meta.totalPages}</p><div className="flex gap-2"><button disabled={page<=1} onClick={()=>setPage(x=>x-1)} className="rounded-lg border p-2 disabled:opacity-40"><FiChevronLeft/></button><button disabled={page>=meta.totalPages} onClick={()=>setPage(x=>x+1)} className="rounded-lg border p-2 disabled:opacity-40"><FiChevronRight/></button></div></div></section></div></DashboardShell>;
}
